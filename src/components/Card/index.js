import React from "react";
import styled from "styled-components";

const Par = styled.p`
	font-size: 1rem;
`;

const Card = () => {
	return (
		<>
			<Par>Hola Sexy</Par>
		</>
	);
};

export default Card;
